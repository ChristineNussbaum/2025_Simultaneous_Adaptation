Word:AdaptType + tML_sc:Word:AdaptType +
+ (1|SpID) + (1 | Participant),
data = E3_Adapt,
family = bernoulli(link = "logit"),  # Defines Logistic Regression
prior = default_prior,
chains = 4, iter = 2000, warmup = 1000, cores = 4,
save_pars = save_pars(all = TRUE)) # to allow Bayes factor estimation later
#Model without the interaction
default_prior2 <- default_prior(Resp ~ tML_sc + AdaptType + Word + tML_sc:Word  + tML_sc:AdaptType
+ (1|SpID) + (1 | Participant),
data = E3_Adapt,  family = bernoulli(link = "logit"))
#in the reduced model, I removed both:  Word:AdaptType AND tML_sc:Word:AdaptType
redmodel <- brm(Resp ~ tML_sc + AdaptType + Word + tML_sc:Word  + tML_sc:AdaptType
+ (1|SpID) + (1 | Participant),
data = E3_Adapt,
family = bernoulli(link = "logit"),  # Defines Logistic Regression
prior = default_prior2,
chains = 4, iter = 2000, warmup = 1000, cores = 4,
save_pars = save_pars(all = TRUE))
bayes_factor(fullmodel, redmodel, )
bayes_factor(fullmodel, redmodel)
bayes_factor(fullmodel, redmodel)
redmodel
bayes_factor(fullmodel, redmodel)
#Model without the interaction
default_prior2 <- default_prior(Resp ~ tML_sc + AdaptType + Word + tML_sc:Word  + tML_sc:AdaptType
+ (1|SpID) + (1 | Participant),
data = E3_Adapt,  family = bernoulli(link = "logit"))
#in the reduced model, I removed both:  Word:AdaptType AND tML_sc:Word:AdaptType
redmodel <- brm(Resp ~ tML_sc + AdaptType + Word + tML_sc:Word  + tML_sc:AdaptType + (1|SpID) + (1 | Participant),
data = E3_Adapt,
family = bernoulli(link = "logit"),  # Defines Logistic Regression
prior = default_prior2,
chains = 4, iter = 2000, warmup = 1000, cores = 4,
save_pars = save_pars(all = TRUE))
bayes_factor(fullmodel, redmodel)
##get derault priors first
default_prior <- default_prior(Resp ~ tML_sc + AdaptType + Word + tML_sc:Word  + tML_sc:AdaptType +
Word:AdaptType + tML_sc:Word:AdaptType +
+ (1|SpID) + (1 | Participant),
data = E3_Adapt,  family = bernoulli(link = "logit"))
#Full Model
fullmodel <- brm(Resp ~ tML_sc + AdaptType + Word + tML_sc:Word  + tML_sc:AdaptType +
Word:AdaptType + tML_sc:Word:AdaptType
+ (1|SpID) + (1 | Participant),
data = E3_Adapt,
family = bernoulli(link = "logit"),  # Defines Logistic Regression
prior = default_prior,
chains = 4, iter = 2000, warmup = 1000, cores = 4,
save_pars = save_pars(all = TRUE)) # to allow Bayes factor estimation later
bayes_factor(fullmodel, redmodel)
#Model without the interaction
default_prior2 <- default_prior(Resp ~ tML_sc + AdaptType + Word + tML_sc:Word  + tML_sc:AdaptType + tML_sc:Word:AdaptType
+ (1|SpID) + (1 | Participant),
data = E3_Adapt,  family = bernoulli(link = "logit"))
#in the reduced model, I removed both:  Word:AdaptType AND tML_sc:Word:AdaptType
redmodel <- brm(Resp ~ tML_sc + AdaptType + Word + tML_sc:Word  + tML_sc:AdaptType + + tML_sc:Word:AdaptType  + (1|SpID) + (1 | Participant),
data = E3_Adapt,
family = bernoulli(link = "logit"),  # Defines Logistic Regression
prior = default_prior2,
chains = 4, iter = 2000, warmup = 1000, cores = 4,
save_pars = save_pars(all = TRUE))
#Model without the interaction
default_prior2 <- default_prior(Resp ~ tML_sc + AdaptType + Word + tML_sc:Word  + tML_sc:AdaptType + tML_sc:Word:AdaptType
+ (1|SpID) + (1 | Participant),
data = E3_Adapt,  family = bernoulli(link = "logit"))
#in the reduced model, I removed both:  Word:AdaptType AND tML_sc:Word:AdaptType
redmodel <- brm(Resp ~ tML_sc + AdaptType + Word + tML_sc:Word  + tML_sc:AdaptType +tML_sc:Word:AdaptType  + (1|SpID) + (1 | Participant),
data = E3_Adapt,
family = bernoulli(link = "logit"),  # Defines Logistic Regression
prior = default_prior2,
chains = 4, iter = 2000, warmup = 1000, cores = 4,
save_pars = save_pars(all = TRUE))
#15 divergent transitions
# thats acceptable as well
bayes_factor(fullmodel, redmodel)
bayes_factor(redmodel, fullmodel)
#set seed
set.seed(42)
bayes_factor(fullmodel, redmodel)
#save the models and the bayes_factors
save(fullmodel, redmodel, file = "input/bayesmodels.RData")
BF <-bayes_factor(fullmodel, redmodel)
capture.output(BF, file = "output/bayesfactor.csv")
capture.output(BF, file = "output/bayesfactor.txt")
# clear directory
rm(list=ls())
#set working directory to current file
setwd(dirname(rstudioapi::getActiveDocumentContext()$path))
# load required packages
library("tidyverse")
library(ez)
library(effectsize)
library(lme4)           # version 1.1-35.1
library(ggeffects)      # version 1.6.0
library(afex)           # version 1.3-1
library(brms)           # for Bayesian estimation
# load relevant functions
source("functions/mySummary.R")
source("functions/tracedEzOut.R")
#set seed
set.seed(42)
#load the data
load(file ="input/Exp3_without_omissions.RData")
## declare tML as numerical variable
E3_Adapt$tML <- as.numeric(as.character(E3_Adapt$tML))
#scale tML, because this is recommended for mixed-effects modelling
E3_Adapt$tML_sc <- scale(E3_Adapt$tML)[,1]
E3_Adapt$Word <- ifelse(E3_Adapt$Word == "w05", "w04", E3_Adapt$Word)
E3_Bline$Word <- ifelse(E3_Bline$Word == "w05", "w04", E3_Bline$Word)
# m: assumed fixed effects structure for Participant and SpID
m <- glmer(Resp ~ tML_sc * Word * AdaptType  + (1|SpID) + (1 | Participant), data = E3_Adapt, family = binomial,
control=glmerControl(optimizer="bobyqa",optCtrl=list(maxfun=2e5))) # it was recommended to try out different optimizers to avoid converging failure and this worked
#------------------------------------------------------------------------
# Step2: calculating the p-values of the effects with afex
m_test<- mixed(m, data = E3_Adapt, method = "LRT", family = "binomial") # takes a few minutes to estimate
m_test
##get default priors first
default_prior <- default_prior(Resp ~ tML_sc + AdaptType + Word + tML_sc:Word  + tML_sc:AdaptType +
Word:AdaptType + tML_sc:Word:AdaptType + (1|SpID) + (1 | Participant),
data = E3_Adapt,  family = bernoulli(link = "logit"))
View(default_prior)
##get default priors first
default_prior <- default_prior(Resp ~ tML_sc + AdaptType + Word + tML_sc:Word  + tML_sc:AdaptType +
Word:AdaptType + tML_sc:Word:AdaptType + (1|SpID) + (1 | Participant),
data = E3_Adapt,  family = bernoulli(link = "logit"))
fullmodel <- brm(Resp ~ tML_sc +  Word + AdaptType + tML_sc:Word  + tML_sc:AdaptType +
Word:AdaptType + tML_sc:Word:AdaptType + (1|SpID) + (1 | Participant),
data = E3_Adapt,
family = bernoulli(link = "logit"),  # Defines Logistic Regression
prior = default_prior,
chains = 4, iter = 2000, warmup = 1000, cores = 4, #set to recommended values
save_pars = save_pars(all = TRUE)) # to allow Bayes factor estimation later
##get default priors first
default_prior <- default_prior(Resp ~ tML_sc +  Word + AdaptType + tML_sc:Word  + tML_sc:AdaptType +
Word:AdaptType + tML_sc:Word:AdaptType + (1|SpID) + (1 | Participant),
data = E3_Adapt,  family = bernoulli(link = "logit"))
fullmodel <- brm(Resp ~ tML_sc +  Word + AdaptType + tML_sc:Word  + tML_sc:AdaptType +
Word:AdaptType + tML_sc:Word:AdaptType + (1|SpID) + (1 | Participant),
data = E3_Adapt,
family = bernoulli(link = "logit"),  # Defines Logistic Regression
prior = default_prior,
chains = 4, iter = 2000, warmup = 1000, cores = 4, #set to recommended values
save_pars = save_pars(all = TRUE)) # to allow Bayes factor estimation later
22/1000
##get default priors again
default_prior2 <- default_prior(Resp ~ tML_sc + AdaptType + Word + tML_sc:Word  + tML_sc:AdaptType + tML_sc:Word:AdaptType
+ (1|SpID) + (1 | Participant),
data = E3_Adapt,  family = bernoulli(link = "logit"))
#all effects are estimated, except Word:AdaptType
redmodel <- brm(Resp ~ tML_sc + AdaptType + Word + tML_sc:Word  + tML_sc:AdaptType
+ tML_sc:Word:AdaptType  + (1|SpID) + (1 | Participant),
data = E3_Adapt,
family = bernoulli(link = "logit"),
prior = default_prior2,
chains = 4, iter = 2000, warmup = 1000, cores = 4,
save_pars = save_pars(all = TRUE))
BF <-bayes_factor(fullmodel, redmodel)
BF
#save the models and the bayes_factors
save(fullmodel, redmodel, file = "input/bayesmodels.RData")
capture.output(BF, file = "output/bayesfactor.txt")
#aggregate across pseudoword, AdaptType for analysis
E3_Resp <- mySummary(E3_Adapt, Resp, Participant, Word, AdaptType) #does not contain Baseline
a<-ezANOVA(data=E3_Resp, dv=.(Resp), wid=.(Participant), within = .(AdaptType, Word), type=3, detailed = TRUE)
b = tracedEzOut(a, print = TRUE, sph.cor = "HF", mau.p = 0.05, etasq = "partial", dfsep = ", ")
#aggregate Data (in this case not really necessary)
PH1 <- E3_Resp %>% group_by(Participant, AdaptType) %>% summarise(Resp = mean(Resp))
#get descriptive data
PH1_descriptive <- mySummary(PH1, Resp, AdaptType)
View(PH1)
View(PH1_descriptive)
#convert partly into wide format
PH1 <- spread(PH1, AdaptType, Resp)
###now we add the Baseline information
E3_Bline_Resp <- mySummary(E3_Bline, Resp, Participant)
E3_Bline_Resp <- E3_Bline_Resp %>% rename(Baseline = Resp)
Baseline_descriptive <- mySummary(E3_Bline_Resp, Baseline)
View(Baseline_descriptive)
#merge
PH1 <- merge(PH1, E3_Bline_Resp)
View(PH1)
View(PH1)
rm(E3_Bline_Resp)
#########
#ttests
AdaptType <- t.test(PH1$`pw12_ang/pw34_fea`, PH1$`pw12_fea/pw34_ang`, paired = TRUE)
d_AdaptType <- t_to_d(AdaptType_PH1$statistic, AdaptType_PH1$parameter, paired = TRUE)
#########
#ttests
AdaptType <- t.test(PH1$`pw12_ang/pw34_fea`, PH1$`pw12_fea/pw34_ang`, paired = TRUE)
d_AdaptType <- t_to_d(AdaptType$statistic, AdaptType$parameter, paired = TRUE)
d_AdaptType
AdaptType
AdaptType_b <- t.test(PH1$Baseline, PH1$`pw12_ang/pw34_fea`, paired = TRUE)
AdaptType_b <- t.test(PH1$Baseline, PH1$`pw12_ang/pw34_fea`, paired = TRUE)
d_AdaptType_b <- t_to_d(AdaptType_b$statistic, AdaptType_b$parameter, paired = TRUE)
AdaptType_b
AdaptType_c <- t.test(PH1$Baseline, PH1$`pw12_fea/pw34_ang`, paired = TRUE)
d_AdaptType_c <- t_to_d(AdaptType_c$statistic, AdaptType_c$parameter, paired = TRUE)
AdaptType_c <- t.test(PH1$Baseline, PH1$`pw12_fea/pw34_ang`, paired = TRUE)
d_AdaptType_c <- t_to_d(AdaptType_c$statistic, AdaptType_c$parameter, paired = TRUE)
AdaptType_c
#save results
capture.output(as.matrix(PH1_descriptive), as.matrix(Baseline_descriptive),
AdaptType,d_AdaptType,
AdaptType_b, d_AdaptType_b,
AdaptType_c, d_AdaptType_c,
file = "output/Exp3_Resp_post-hoc_PH1.txt")
#keep environment tidy
rm(PH1_descriptive, Baseline_descriptive, AdaptType,d_AdaptType, AdaptType_b, d_AdaptType_b,
AdaptType_c, d_AdaptType_c)
# clear directory
rm(list=ls())
#set working directory to current file
setwd(dirname(rstudioapi::getActiveDocumentContext()$path))
# load required packages
library("tidyverse")
library(ez)
library(effectsize)
library(lme4)           # version 1.1-35.1
library(ggeffects)      # version 1.6.0
library(afex)           # version 1.3-1
library(brms)           # for Bayesian estimation
# load relevant functions
source("functions/mySummary.R")
source("functions/tracedEzOut.R")
#set seed
set.seed(42)
#load the data
load(file ="input/Exp3_without_omissions.RData")
E3_Adapt_f <- E3_Adapt %>% filter(SpSex == "f")
## declare tML as numerical variable
E3_Adapt_f$tML <- as.numeric(as.character(E3_Adapt_f$tML))
#scale tML, because this is recommended for mixed-effects modelling
E3_Adapt_f$tML_sc <- scale(E3_Adapt_f$tML)[,1]
E3_Adapt_f$Word <- ifelse(E3_Adapt_f$Word == "w05", "w04", E3_Adapt_f$Word)
E3_Bline_f$Word <- ifelse(E3_Bline_f$Word == "w05", "w04", E3_Bline_f$Word)
E3_Bline_f <- E3_Bline %>% filter(SpSex == "f")
View(E3_Adapt_f)
View(E3_Bline_f)
## declare tML as numerical variable
E3_Adapt_f$tML <- as.numeric(as.character(E3_Adapt_f$tML))
#scale tML, because this is recommended for mixed-effects modelling
E3_Adapt_f$tML_sc <- scale(E3_Adapt_f$tML)[,1]
E3_Adapt_f$Word <- ifelse(E3_Adapt_f$Word == "w05", "w04", E3_Adapt_f$Word)
E3_Bline_f$Word <- ifelse(E3_Bline_f$Word == "w05", "w04", E3_Bline_f$Word)
# m: assumed fixed effects structure for Participant and SpID
m <- glmer(Resp ~ tML_sc * Word * AdaptType  + (1|SpID) + (1 | Participant), data = E3_Adapt_f, family = binomial,
control=glmerControl(optimizer="bobyqa",optCtrl=list(maxfun=2e5))) # it was recommended to try out different optimizers to avoid converging failure and this worked
#------------------------------------------------------------------------
# Step2: calculating the p-values of the effects with afex
m_test<- mixed(m, data = E3_Adapt_f, method = "LRT", family = "binomial") # takes a few minutes to estimate
m_test
summary(m)
# save models as R objects
save(m, m_test, file= "input/E3_GLMs_females.RData")
# capture output of model "m"
capture.output(summary(m), m_test, file= "output/Exp3_GLMM_females.txt")
#-------------------------------------------------------------------------------#
#   Analysis 1b: run the same with brms for estimation of the Baysian effect    #
#-------------------------------------------------------------------------------#
rm(E3_Adapt)
##get default priors first
default_prior <- default_prior(Resp ~ tML_sc +  Word + AdaptType + tML_sc:Word  + tML_sc:AdaptType +
Word:AdaptType + tML_sc:Word:AdaptType + (1|SpID) + (1 | Participant),
data = E3_Adapt_f,  family = bernoulli(link = "logit"))
fullmodel <- brm(Resp ~ tML_sc +  Word + AdaptType + tML_sc:Word  + tML_sc:AdaptType +
Word:AdaptType + tML_sc:Word:AdaptType + (1|SpID) + (1 | Participant),
data = E3_Adapt_f,
family = bernoulli(link = "logit"),  # Defines Logistic Regression
prior = default_prior,
chains = 4, iter = 2000, warmup = 1000, cores = 4, #set to recommended values
save_pars = save_pars(all = TRUE)) # to allow Bayes factor estimation later
##get default priors first
default_prior <- default_prior(Resp ~ tML_sc +  Word + AdaptType + Word:AdaptType + (1|SpID) + (1 | Participant),
data = E3_Adapt_f,  family = bernoulli(link = "logit"))
fullmodel <- brm(Resp ~ tML_sc +  Word + AdaptType + Word:AdaptType + (1|SpID) + (1 | Participant),
data = E3_Adapt_f,
family = bernoulli(link = "logit"),  # Defines Logistic Regression
prior = default_prior,
chains = 4, iter = 2000, warmup = 1000, cores = 4, #set to recommended values
save_pars = save_pars(all = TRUE)) # to allow Bayes factor estimation later
##get default priors again
default_prior2 <- default_prior(Resp ~ tML_sc +  Word + AdaptType + (1|SpID) + (1 | Participant),
data = E3_Adapt_f,  family = bernoulli(link = "logit"))
#all effects are estimated, except Word:AdaptType
redmodel <- brm(Resp ~ tML_sc +  Word + AdaptType +  (1|SpID) + (1 | Participant),
data = E3_Adapt_f,
family = bernoulli(link = "logit"),
prior = default_prior2,
chains = 4, iter = 2000, warmup = 1000, cores = 4,
save_pars = save_pars(all = TRUE))
BF <-bayes_factor(fullmodel, redmodel)
BF
#save the models and the bayes_factors
save(fullmodel, redmodel, file = "input/bayesmodels_female.RData")
capture.output(BF, file = "output/bayesfactor_female.txt")
#aggregate across pseudoword, AdaptType for analysis
E3_Resp <- mySummary(E3_Adapt_f, Resp, Participant, Word, AdaptType) #does not contain Baseline
a<-ezANOVA(data=E3_Resp, dv=.(Resp), wid=.(Participant), within = .(AdaptType, Word), type=3, detailed = TRUE)
b = tracedEzOut(a, print = TRUE, sph.cor = "HF", mau.p = 0.05, etasq = "partial", dfsep = ", ")
#aggregate Data (in this case not really necessary)
PH1 <- E3_Resp %>% group_by(Participant, AdaptType) %>% summarise(Resp = mean(Resp))
#get descriptive data
PH1_descriptive <- mySummary(PH1, Resp, AdaptType)
#convert partly into wide format
PH1 <- spread(PH1, AdaptType, Resp)
###now we add the Baseline information
E3_Bline_f_Resp <- mySummary(E3_Bline_f, Resp, Participant)
E3_Bline_f_Resp <- E3_Bline_f_Resp %>% rename(Baseline = Resp)
Baseline_descriptive <- mySummary(E3_Bline_f_Resp, Baseline)
#merge
PH1 <- merge(PH1, E3_Bline_f_Resp)
rm(E3_Bline_f_Resp)
#########
#ttests
AdaptType <- t.test(PH1$`pw12_ang/pw34_fea`, PH1$`pw12_fea/pw34_ang`, paired = TRUE)
d_AdaptType <- t_to_d(AdaptType$statistic, AdaptType$parameter, paired = TRUE)
AdaptType
AdaptType_b <- t.test(PH1$Baseline, PH1$`pw12_ang/pw34_fea`, paired = TRUE)
d_AdaptType_b <- t_to_d(AdaptType_b$statistic, AdaptType_b$parameter, paired = TRUE)
AdaptType_b
AdaptType_c <- t.test(PH1$Baseline, PH1$`pw12_fea/pw34_ang`, paired = TRUE)
d_AdaptType_c <- t_to_d(AdaptType_c$statistic, AdaptType_c$parameter, paired = TRUE)
AdaptType_c
#save results
capture.output(as.matrix(PH1_descriptive), as.matrix(Baseline_descriptive),
AdaptType,d_AdaptType,
AdaptType_b, d_AdaptType_b,
AdaptType_c, d_AdaptType_c,
file = "output/Exp3_Resp_post-hoc_PH1_females.txt")
#keep environment tidy
rm(PH1_descriptive, Baseline_descriptive, AdaptType,d_AdaptType, AdaptType_b, d_AdaptType_b,
AdaptType_c, d_AdaptType_c)
# clear directory
rm(list=ls())
#set working directory to current file
setwd(dirname(rstudioapi::getActiveDocumentContext()$path))
# load required packages
library("tidyverse")
library(ez)
library(effectsize)
library(lme4)           # version 1.1-35.1
library(ggeffects)      # version 1.6.0
library(afex)           # version 1.3-1
library(brms)           # for Bayesian estimation
# load relevant functions
source("functions/mySummary.R")
source("functions/tracedEzOut.R")
#set seed
set.seed(42)
#set seed
set.seed(42)
#load the data
load(file ="input/Exp3_without_omissions.RData")
E3_Adapt_m <- E3_Adapt %>% filter(SpSex == "f")
E3_Adapt_m <- E3_Adapt %>% filter(SpSex == "f")
E3_Bline_m <- E3_Bline %>% filter(SpSex == "f")
## declare tML as numerical variable
E3_Adapt_m$tML <- as.numeric(as.character(E3_Adapt_m$tML))
#scale tML, because this is recommended for mixed-effects modelling
E3_Adapt_m$tML_sc <- scale(E3_Adapt_m$tML)[,1]
E3_Adapt_m$Word <- ifelse(E3_Adapt_m$Word == "w05", "w04", E3_Adapt_m$Word)
E3_Bline_m$Word <- ifelse(E3_Bline_m$Word == "w05", "w04", E3_Bline_m$Word)
# m: assumed fixed effects structure for Participant and SpID
m <- glmer(Resp ~ tML_sc * Word * AdaptType  + (1|SpID) + (1 | Participant), data = E3_Adapt_m, family = binomial,
control=glmerControl(optimizer="bobyqa",optCtrl=list(maxfun=2e5))) # it was recommended to try out different optimizers to avoid converging failure and this worked
#------------------------------------------------------------------------
# Step2: calculating the p-values of the effects with afex
m_test<- mixed(m, data = E3_Adapt_m, method = "LRT", family = "binomial") # takes a few minutes to estimate
m_test
# save models as R objects
save(m, m_test, file= "input/E3_GLMs_males.RData")
# capture output of model "m"
capture.output(summary(m), m_test, file= "output/Exp3_GLMM_males.txt")
##get default priors first
default_prior <- default_prior(Resp ~ tML_sc +  Word + AdaptType + Word:AdaptType + (1|SpID) + (1 | Participant),
data = E3_Adapt_m,  family = bernoulli(link = "logit"))
fullmodel <- brm(Resp ~ tML_sc +  Word + AdaptType + Word:AdaptType + (1|SpID) + (1 | Participant),
data = E3_Adapt_m,
family = bernoulli(link = "logit"),  # Defines Logistic Regression
prior = default_prior,
chains = 4, iter = 2000, warmup = 1000, cores = 4, #set to recommended values
save_pars = save_pars(all = TRUE)) # to allow Bayes factor estimation later
##get default priors again
default_prior2 <- default_prior(Resp ~ tML_sc +  Word + AdaptType + (1|SpID) + (1 | Participant),
data = E3_Adapt_m,  family = bernoulli(link = "logit"))
#all effects are estimated, except Word:AdaptType
redmodel <- brm(Resp ~ tML_sc +  Word + AdaptType +  (1|SpID) + (1 | Participant),
data = E3_Adapt_m,
family = bernoulli(link = "logit"),
prior = default_prior2,
chains = 4, iter = 2000, warmup = 1000, cores = 4,
save_pars = save_pars(all = TRUE))
BF <-bayes_factor(fullmodel, redmodel)
BF
#save the models and the bayes_factors
save(fullmodel, redmodel, file = "input/bayesmodels_male.RData")
capture.output(BF, file = "output/bayesfactor_male.txt")
#aggregate across pseudoword, AdaptType for analysis
E3_Resp <- mySummary(E3_Adapt_m, Resp, Participant, Word, AdaptType) #does not contain Baseline
a<-ezANOVA(data=E3_Resp, dv=.(Resp), wid=.(Participant), within = .(AdaptType, Word), type=3, detailed = TRUE)
b = tracedEzOut(a, print = TRUE, sph.cor = "HF", mau.p = 0.05, etasq = "partial", dfsep = ", ")
# clear directory
rm(list=ls())
#set working directory to current file
setwd(dirname(rstudioapi::getActiveDocumentContext()$path))
# load required packages
library("tidyverse")
library(ez)
library(effectsize)
library(lme4)           # version 1.1-35.1
library(ggeffects)      # version 1.6.0
library(afex)           # version 1.3-1
library(brms)           # for Bayesian estimation
# load relevant functions
source("functions/mySummary.R")
source("functions/tracedEzOut.R")
#set seed
set.seed(42)
#load the data
load(file ="input/Exp3_without_omissions.RData")
E3_Adapt_m <- E3_Adapt %>% filter(SpSex == "m")
E3_Bline_m <- E3_Bline %>% filter(SpSex == "m")
## declare tML as numerical variable
E3_Adapt_m$tML <- as.numeric(as.character(E3_Adapt_m$tML))
#scale tML, because this is recommended for mixed-effects modelling
E3_Adapt_m$tML_sc <- scale(E3_Adapt_m$tML)[,1]
E3_Adapt_m$Word <- ifelse(E3_Adapt_m$Word == "w05", "w04", E3_Adapt_m$Word)
E3_Bline_m$Word <- ifelse(E3_Bline_m$Word == "w05", "w04", E3_Bline_m$Word)
# m: assumed fixed effects structure for Participant and SpID
m <- glmer(Resp ~ tML_sc * Word * AdaptType  + (1|SpID) + (1 | Participant), data = E3_Adapt_m, family = binomial,
control=glmerControl(optimizer="bobyqa",optCtrl=list(maxfun=2e5))) # it was recommended to try out different optimizers to avoid converging failure and this worked
#------------------------------------------------------------------------
# Step2: calculating the p-values of the effects with afex
m_test<- mixed(m, data = E3_Adapt_m, method = "LRT", family = "binomial") # takes a few minutes to estimate
m_test
# save models as R objects
save(m, m_test, file= "input/E3_GLMs_males.RData")
# capture output of model "m"
capture.output(summary(m), m_test, file= "output/Exp3_GLMM_males.txt")
##get default priors first
default_prior <- default_prior(Resp ~ tML_sc +  Word + AdaptType + Word:AdaptType + (1|SpID) + (1 | Participant),
data = E3_Adapt_m,  family = bernoulli(link = "logit"))
fullmodel <- brm(Resp ~ tML_sc +  Word + AdaptType + Word:AdaptType + (1|SpID) + (1 | Participant),
data = E3_Adapt_m,
family = bernoulli(link = "logit"),  # Defines Logistic Regression
prior = default_prior,
chains = 4, iter = 2000, warmup = 1000, cores = 4, #set to recommended values
save_pars = save_pars(all = TRUE)) # to allow Bayes factor estimation later
##get default priors again
default_prior2 <- default_prior(Resp ~ tML_sc +  Word + AdaptType + (1|SpID) + (1 | Participant),
data = E3_Adapt_m,  family = bernoulli(link = "logit"))
#all effects are estimated, except Word:AdaptType
redmodel <- brm(Resp ~ tML_sc +  Word + AdaptType +  (1|SpID) + (1 | Participant),
data = E3_Adapt_m,
family = bernoulli(link = "logit"),
prior = default_prior2,
chains = 4, iter = 2000, warmup = 1000, cores = 4,
save_pars = save_pars(all = TRUE))
BF <-bayes_factor(fullmodel, redmodel)
BF
#save the models and the bayes_factors
save(fullmodel, redmodel, file = "input/bayesmodels_male.RData")
capture.output(BF, file = "output/bayesfactor_male.txt")
#aggregate across pseudoword, AdaptType for analysis
E3_Resp <- mySummary(E3_Adapt_m, Resp, Participant, Word, AdaptType) #does not contain Baseline
a<-ezANOVA(data=E3_Resp, dv=.(Resp), wid=.(Participant), within = .(AdaptType, Word), type=3, detailed = TRUE)
b = tracedEzOut(a, print = TRUE, sph.cor = "HF", mau.p = 0.05, etasq = "partial", dfsep = ", ")
#aggregate Data (in this case not really necessary)
PH1 <- E3_Resp %>% group_by(Participant, AdaptType) %>% summarise(Resp = mean(Resp))
#get descriptive data
PH1_descriptive <- mySummary(PH1, Resp, AdaptType)
#convert partly into wide format
PH1 <- spread(PH1, AdaptType, Resp)
###now we add the Baseline information
E3_Bline_m_Resp <- mySummary(E3_Bline_m, Resp, Participant)
E3_Bline_m_Resp <- E3_Bline_m_Resp %>% rename(Baseline = Resp)
Baseline_descriptive <- mySummary(E3_Bline_m_Resp, Baseline)
#merge
PH1 <- merge(PH1, E3_Bline_m_Resp)
rm(E3_Bline_m_Resp)
#########
#ttests
AdaptType <- t.test(PH1$`pw12_ang/pw34_fea`, PH1$`pw12_fea/pw34_ang`, paired = TRUE)
d_AdaptType <- t_to_d(AdaptType$statistic, AdaptType$parameter, paired = TRUE)
AdaptType
AdaptType_b <- t.test(PH1$Baseline, PH1$`pw12_ang/pw34_fea`, paired = TRUE)
d_AdaptType_b <- t_to_d(AdaptType_b$statistic, AdaptType_b$parameter, paired = TRUE)
AdaptType_b
AdaptType_c <- t.test(PH1$Baseline, PH1$`pw12_fea/pw34_ang`, paired = TRUE)
d_AdaptType_c <- t_to_d(AdaptType_c$statistic, AdaptType_c$parameter, paired = TRUE)
AdaptType_c
#save results
capture.output(as.matrix(PH1_descriptive), as.matrix(Baseline_descriptive),
AdaptType,d_AdaptType,
AdaptType_b, d_AdaptType_b,
AdaptType_c, d_AdaptType_c,
file = "output/Exp3_Resp_post-hoc_PH1_females.txt")
#keep environment tidy
rm(PH1_descriptive, Baseline_descriptive, AdaptType,d_AdaptType, AdaptType_b, d_AdaptType_b,
AdaptType_c, d_AdaptType_c)
#save results
capture.output(as.matrix(PH1_descriptive), as.matrix(Baseline_descriptive),
AdaptType,d_AdaptType,
AdaptType_b, d_AdaptType_b,
AdaptType_c, d_AdaptType_c,
file = "output/Exp3_Resp_post-hoc_PH1_males.txt")
#keep environment tidy
rm(PH1_descriptive, Baseline_descriptive, AdaptType,d_AdaptType, AdaptType_b, d_AdaptType_b,
AdaptType_c, d_AdaptType_c)
